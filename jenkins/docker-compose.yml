version: '3'
services:
  jenkins:
    build:
      context: .
    user: 'root'
    ports:
      - '9080:9080'
      - '50000:50000'
    volumes:
      - './jenkins_home:/var/jenkins_home'
      - '/var/run/docker.sock:/var/run/docker.sock'

volumes:
  jenkins_home:
